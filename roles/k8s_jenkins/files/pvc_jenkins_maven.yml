apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-jenkins-maven
  namespace: jenkins
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: ""
  volumeName: pv-jenkins-maven
