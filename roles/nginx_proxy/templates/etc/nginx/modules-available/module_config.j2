stream {
    server {
        listen 80;
        proxy_pass {{ nginx_domain_ip }}:80;
        #proxy_timeout 3s;
        #proxy_connect_timeout 1s;
    }

    server {
        listen 443;
        proxy_pass {{ nginx_domain_ip }}:443;
        #proxy_timeout 3s;
        #proxy_connect_timeout 1s;
    }

    server {
        listen 6443;
        proxy_pass {{ nginx_domain_ip }}:6443;
        #proxy_timeout 3s;
        #proxy_connect_timeout 1s;
    }

    #server {
    #    listen 22;
    #    proxy_pass {{ nginx_domain_ip }}:22;
    #    proxy_timeout 3s;
    #    proxy_connect_timeout 1s;
    #}
}
